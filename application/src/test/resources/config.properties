#this is dynamic because we need it in the ModuleInitializerBean and in the USM deployment descriptor
module.name=activityModule

#USM related properties
usm.deployment.descriptor.force-update=true
usm.rest.server=http://localhost:8089
usm.admin.rest.username=usm_bootstrap
usm.admin.rest.password=password
